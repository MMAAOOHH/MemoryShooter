#include "System.h"

/*
 
 void ECS::System::add_entity(const Entity_ID entity)
{
	entities.insert(entity);
}
void ECS::System::remove_entity(const Entity_ID entity)
{
	entities.erase(entity);
}

ECS::Entity_Signature ECS::System::get_signature() const
{
	return signature;
}

*/
